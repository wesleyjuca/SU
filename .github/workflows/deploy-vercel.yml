name: 🔼 Deploy Frontend (Vercel)

on:
  workflow_dispatch:
    inputs:
      vercel_token:
        description: 'Vercel Personal Access Token (vercel.com/account/tokens)'
        required: true
        type: string
      railway_url:
        description: 'Railway backend URL (ex: https://afj-core-production.up.railway.app)'
        required: false
        default: ''
        type: string

jobs:
  deploy:
    name: Deploy para Vercel
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: claude/afj-core-system-OlGA2

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Mask token in logs
        run: echo "::add-mask::${{ github.event.inputs.vercel_token }}"

      - name: Install Vercel CLI
        run: npm install -g vercel

      - name: Configure NEXT_PUBLIC_API_URL
        if: github.event.inputs.railway_url != ''
        env:
          VERCEL_TOKEN: ${{ github.event.inputs.vercel_token }}
        run: |
          RAILWAY_URL="${{ github.event.inputs.railway_url }}"
          echo "Configurando NEXT_PUBLIC_API_URL=$RAILWAY_URL..."

          # Remove env var se existir (ignora erro se não existir)
          vercel env rm NEXT_PUBLIC_API_URL production --yes --token "$VERCEL_TOKEN" 2>/dev/null || true

          # Adiciona env var
          echo "$RAILWAY_URL" | vercel env add NEXT_PUBLIC_API_URL production --token "$VERCEL_TOKEN" --yes 2>&1 || \
            echo "::warning::Failed to set env var via CLI — set manually on Vercel dashboard"

      - name: Deploy to Vercel (production)
        env:
          VERCEL_TOKEN: ${{ github.event.inputs.vercel_token }}
        run: |
          echo "Iniciando deploy no Vercel..."
          DEPLOY_OUTPUT=$(vercel --yes --prod --token "$VERCEL_TOKEN" 2>&1)
          EXIT_CODE=$?
          echo "$DEPLOY_OUTPUT"

          if [ $EXIT_CODE -eq 0 ]; then
            DEPLOY_URL=$(echo "$DEPLOY_OUTPUT" | grep -oP 'https://[a-zA-Z0-9._-]+\.vercel\.app' | tail -1 || echo "")
            echo ""
            echo "════════════════════════════════════════════"
            echo "  ✅ FRONTEND PUBLICADO!"
            echo "  URL: $DEPLOY_URL"
            echo "════════════════════════════════════════════"
            echo "VERCEL_URL=$DEPLOY_URL" >> $GITHUB_ENV
          else
            echo "::error::Deploy falhou. Verifique os logs acima."
            exit 1
          fi

      - name: Update Railway CORS with Vercel URL
        if: env.VERCEL_URL != ''
        env:
          RAILWAY_TOKEN: aa2353a4-96fb-4f34-ae0c-ec8895ffebaf
        run: |
          if [ -z "$VERCEL_URL" ]; then
            exit 0
          fi

          echo "Atualizando CORS no Railway com: $VERCEL_URL"
          npm install -g @railway/cli 2>/dev/null

          cd backend
          railway variables set \
            CORS_ORIGINS="[\"$VERCEL_URL\",\"http://localhost:3000\"]" \
            2>&1 || echo "::warning::CORS update failed — update manually no Railway dashboard"

          echo ""
          echo "════════════════════════════════════════════"
          echo "  ✅ DEPLOY COMPLETO!"
          echo "  Frontend: $VERCEL_URL"
          echo "  Backend:  ${{ github.event.inputs.railway_url }}"
          echo "  Login:    admin@afjadvogados.com.br"
          echo "  Senha:    Admin@123"
          echo "════════════════════════════════════════════"
